# Credenciales locales (no se suben a Git)

Coloca aquí el JSON de la cuenta de servicio de Google Play:

- Archivo actual usado por EAS: `matchpoint-489600-eb02f870bad7.json` (clave JSON de Google Cloud → IAM → cuenta de servicio → Claves).

`eas.json` → `submit.production.android.serviceAccountKeyPath` apunta a ese fichero. Si renombrás el JSON, actualizá esa ruta.
